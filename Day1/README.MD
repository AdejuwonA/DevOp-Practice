1.	Create a script that Prints the current disk usage (`df -h`).
2.	Lists running processes (`ps aux | head`).
3.	Checks if `httpd` is running (`systemctl is-active httpd`).
4.	Make the script executable and run it.
5.	Save the output as results.
6.	Add the script and output to a Git repository.
