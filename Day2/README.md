1.	Use `journalctl` to view logs for the `httpd` service.
2.	Filter logs from the last 1 hour and save to httpd_logs.txt`.
3.	Write a script `check_logs.sh` to monitor if `httpd` has errors (grep for “error” in logs).
 
4.	Run the script and save its output to `log_analysis.txt`.
5.	Commit both files to a Git repository.
