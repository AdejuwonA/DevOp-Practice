1.	Install and Configure Ansible: Install Ansible on my terminal.
	Verify the installation by checking the Ansible version.
	Create an inventory file to manage my local VM (`localhost`).
2.	Write an Ansible Playbook:
	Create a directory `/home/devop_practice/day3/ansible`. 
	Wrote a playbook `setup_nginx.yml` to install the `nginx` package.
	Starts and enable the `nginx` service.
	Copy a custom HTML file (`index.html`) with content “Welcome to DevOps Training!” to `/usr/share/nginx/html/index.html`.
	Run the playbook and verify Nginx is running by accessing `http://<vm-ip>` or using `curl`.
3.	Version Control the Playbook:
	Initialize a Git repository in my directory.
	Commit the playbook and inventory file to the repository.
